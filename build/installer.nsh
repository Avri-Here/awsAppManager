; Minimal installer configuration for AWS App Manager
; This file provides basic customization without interfering with default NSIS flow

; Main installation section (required but minimal)
Section "MainSection" SEC01
  ; The actual installation is handled by electron-builder's main NSIS script
  ; This section must exist but should be minimal to avoid conflicts
SectionEnd